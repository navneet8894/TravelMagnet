const mongoose=require("mongoose");const schema=new mongoose.Schema({user:{type:mongoose.Schema.Types.ObjectId,ref:"User",required:true,index:true},tokenHash:{type:String,required:true,unique:true},family:{type:String,required:true,index:true},expiresAt:{type:Date,required:true,index:{expires:0}},revokedAt:Date,revokedReason:String,replacedByHash:String,createdByIp:String,lastUsedIp:String,userAgent:String,lastUsedAt:Date},{timestamps:true});schema.index({user:1,revokedAt:1,expiresAt:1});module.exports=mongoose.model("RefreshToken",schema);
