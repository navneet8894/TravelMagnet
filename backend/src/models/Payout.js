const mongoose=require("mongoose");const schema=new mongoose.Schema({vendor:{type:mongoose.Schema.Types.ObjectId,ref:"User",required:true,index:true},bookings:[{type:mongoose.Schema.Types.ObjectId,ref:"Booking"}],grossAmount:{type:Number,required:true},commissionAmount:{type:Number,required:true},gstAmount:{type:Number,required:true},netAmount:{type:Number,required:true},status:{type:String,enum:["pending","scheduled","processing","paid","failed","held"],default:"pending",index:true},scheduledAt:Date,paidAt:Date,providerReference:String,bankAccountMasked:String,internalNote:{type:String,select:false}},{timestamps:true});schema.index({vendor:1,createdAt:-1});module.exports=mongoose.model("Payout",schema);
