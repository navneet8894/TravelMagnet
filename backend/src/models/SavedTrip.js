const mongoose=require("mongoose");const schema=new mongoose.Schema({user:{type:mongoose.Schema.Types.ObjectId,ref:"User",required:true,index:true},title:{type:String,required:true},destination:String,startDate:Date,endDate:Date,budget:Number,travellers:Number,itinerary:{type:mongoose.Schema.Types.Mixed},shareToken:{type:String,unique:true,sparse:true}},{timestamps:true});module.exports=mongoose.model("SavedTrip",schema);
