const express=require("express"),c=require("../controllers/systemController"),{protect,authorize,requirePermission}=require("../middleware/auth");const r=express.Router();r.get("/admin/roles",protect,authorize("admin"),requirePermission("roles.read"),c.roles);r.post("/admin/roles",protect,authorize("admin"),requirePermission("roles.manage"),c.roles);r.put("/admin/roles/:id",protect,authorize("admin"),requirePermission("roles.manage"),c.roles);r.get("/admin/content",protect,authorize("admin"),requirePermission("content.read"),c.content);r.post("/admin/content",protect,authorize("admin"),requirePermission("content.manage"),c.content);r.put("/admin/content/:id",protect,authorize("admin"),requirePermission("content.manage"),c.content);r.get("/admin/fraud-alerts",protect,authorize("admin"),requirePermission("fraud.read"),c.fraud);r.put("/admin/fraud-alerts/:id",protect,authorize("admin"),requirePermission("fraud.manage"),c.fraud);r.get("/owner/tasks",protect,authorize("vendor","admin"),c.tasks);r.post("/owner/tasks",protect,authorize("vendor","admin"),c.tasks);r.put("/owner/tasks/:id",protect,authorize("vendor","admin"),c.tasks);r.post("/coupons/validate",protect,authorize("user"),c.validateCoupon);r.get("/experience-bookings",protect,authorize("user"),c.experienceBooking);r.post("/experience-bookings",protect,authorize("user"),c.experienceBooking);r.post("/ai/itinerary",protect,authorize("user"),c.itinerary);r.post("/ai/pricing-suggestions",protect,authorize("vendor"),c.pricingSuggestion);r.put("/ai/pricing-suggestions/:id",protect,authorize("vendor"),c.decidePricing);r.post("/ai/content",protect,authorize("vendor"),c.contentAssistant);r.get("/ai/review-insights/:hotelId",protect,authorize("vendor"),c.reviewInsights);module.exports=r;
