"use client";import{useEffect,useState}from"react";import{apiRequest}from"@/lib/api";type Summary={hotelCount:number;roomCount:number;availableInventory:number;blockedInventory:number;maintenanceRooms:number;pendingChanges:number;pricingAlerts:number;photoVerificationAlerts:number;inventoryAlerts:number};export default function OwnerAlerts(){const[data,setData]=useState<Summary|null>(null);useEffect(()=>{apiRequest<Summary>("/owner/operations-summary").then(setData).catch(()=>{})},[]);if(!data)return null;return <section className="bg-slate-50 px-5 pb-8 dark:bg-slate-950 lg:ml-64"><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{[["Hotels",data.hotelCount],["Rooms",data.roomCount],["Available today",data.availableInventory],["Blocked",data.blockedInventory],["Maintenance",data.maintenanceRooms]].map(([label,value])=><article key={String(label)} className="tm-card p-4"><b className="text-2xl">{value}</b><p className="text-sm text-slate-500">{label}</p></article>)}</div><div className="mt-4 grid gap-3 sm:grid-cols-4">{[["Pending changes",data.pendingChanges],["Pricing alerts",data.pricingAlerts],["Photo alerts",data.photoVerificationAlerts],["Inventory alerts",data.inventoryAlerts]].map(([label,value])=><article key={String(label)} className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-amber-900"><b>{value}</b><p className="text-sm">{label}</p></article>)}</div></section>}
